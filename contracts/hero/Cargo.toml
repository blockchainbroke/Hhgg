[package]
name = "hero"
version = "0.1.0"
edition = "2021"
description = "NearVerse hero NFT contract (NEP-171, 5 classes)"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
near-sdk = "5.1.0"

[dev-dependencies]
near-sdk = { version = "5.1.0", features = ["unit-testing"] }
