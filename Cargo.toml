[workspace]
members = [
  "contracts/access",
  "contracts/hero",
  "contracts/snapshot",
  # "contracts/veGOLD",   # Batch 2
  # "contracts/pvp",      # Batch 2
]
resolver = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
overflow-checks = true
